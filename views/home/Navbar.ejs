<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Web</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background: #f8f8f8;
        }

        .header {
            background: rgba(0, 0, 0, 0.6);
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 70px;
            padding: 0 80px;
            width: 100%;
            position: fixed;
            top: 0;
            z-index: 100;
        }

        .header h2 {
            color: white;
            font-size: 24px;
            font-weight: 600;
        }

        .header ul {
            display: flex;
            list-style: none;
            gap: 20px;
        }

        .header ul li a {
            text-decoration: none;
            color: white;
            font-size: 16px;
            transition: 0.3s;
            padding-bottom: 5px;
        }

        .header ul li a:hover,
        .header ul li a.active {
            border-bottom: 2px solid green;
            font-weight: 900;
            padding: 10px;
        }

        .right {
            display: flex;
            flex-direction: row-reverse;
            gap: 40px;
        }
        .right button {
            background: transparent;
            border: 2px solid white;
            padding: 10px 20px;
            border-radius: 30px;
            transition: 0.3s;
            font-size: 16px;
            cursor: pointer;
            color: white;
        }

        .right button:hover {
            background: white;
            color: black;
        }

        .cart-btn {
            background-color: transparent;
            border: 2px solid black;
            border-radius: 40px;
            padding: 10px 15px;
            display: flex;
            align-items: center;
            font-size: 16px;
            cursor: pointer;
            position: relative;
        }

        .cart-btn i {
            font-size: 18px;
        }

        .cart-btn .cart-count {
            background-color: red;
            color: white;
            border-radius: 50%;
            font-size: 12px;
            padding: 2px 6px;
            position: absolute;
            top: -5px;
            right: -5px;
        }

        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                height: auto;
                padding: 15px;
                text-align: center;
            }

            .header ul {
                flex-direction: column;
                padding-top: 10px;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="header">
            <div class="header-left">
                <h2>Food Web</h2>
            </div>
            <div class="header-main">
                <ul>
                    <li><a href="home" class="active" onclick="setActive(event)">Home</a></li>
                    <li><a href="Menu" onclick="setActive(event)">Menu</a></li>
                    <li><a href="#" onclick="setActive(event)">Mobile App</a></li>
                    <li><a href="#" onclick="setActive(event)">Contact Us</a></li>
                </ul>
            </div>
            <div class="right">
                
                <button><a href="sign-in" style="color: inherit; text-decoration: none;">Sign In</a></button>
                <button class="cart-btn">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="cart-count">0</span>
                </button>
            </div>
        </div>
    </header>

    <script>
        function setActive(event) {
            document.querySelectorAll('.header .header-main ul li a').forEach(link => {
                link.classList.remove('active');
            });
            event.currentTarget.classList.add('active');
        }
    </script>
</body>
</html>